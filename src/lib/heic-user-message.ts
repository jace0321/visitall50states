/**
 * Single user-facing copy when HEIC→JPEG conversion fails (server or client fetch).
 * Keep in sync with any static HTML that cannot import this module.
 */
export const HEIC_CONVERT_FAILURE_MESSAGE =
  "This HEIC photo variant isn't supported yet. Please export/share it as JPEG from Photos, or switch iPhone Camera → Formats → Most Compatible.";
